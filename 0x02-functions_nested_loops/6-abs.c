#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 *@t:the computes integer
 * Return: the true value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);

}
