#include "main.h"
/**
 *
 * _islower - check if a char is lowercase
 *
 * Return: returns 1 if char is lower case else 0.
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
	else
		return (0);

}
