#include "main.h"
/**
 *
 * a function that checks for alphabetic character
 *
 * Returns 1 if c is a letter, lowercase or uppercase
 */
int _islower(int c);
{
if (c >= 'a' && c <= 'z')
return (1);
	else
		return (0);

}
